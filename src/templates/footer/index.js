import React from 'react'
import PropTypes from 'prop-types'
import { graphql } from 'gatsby'
import Layout from '../../components/Layout'

export const FooterPage = ({ data }) => {
  return <div></div>
}
export default FooterPage
